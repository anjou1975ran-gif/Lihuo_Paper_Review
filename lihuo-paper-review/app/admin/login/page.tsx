import { login } from "./actions";
export default async function Page({ searchParams }: { searchParams: Promise<{error?:string}> }) { const {error}=await searchParams; return <div className="container-shell py-20 max-w-xl"><div className="card p-8"><div className="kicker">Admin Access</div><h1 className="mt-3 text-3xl font-black text-[var(--navy)]">管理員登入</h1><p className="mt-3 muted">不提供公開註冊。帳號必須同時存在於 Supabase Auth、ADMIN_EMAILS 與 admin_users。</p>{error && <p className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-red-800">登入失敗：{decodeURIComponent(error)}</p>}<form action={login} className="mt-7 space-y-5"><div className="field"><label htmlFor="email">管理員電子郵件</label><input className="input" id="email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="password">密碼</label><input className="input" id="password" name="password" type="password" required autoComplete="current-password"/></div><button className="btn btn-primary w-full" type="submit">登入管理後台</button></form></div></div> }
