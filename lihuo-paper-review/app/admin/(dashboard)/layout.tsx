import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { logout } from "@/app/admin/login/actions";
export default async function Layout({children}:{children:React.ReactNode}) { const admin=await requireAdmin(); return <div className="container-shell py-8"><div className="grid lg:grid-cols-[230px_1fr] gap-6"><aside className="card p-4 h-fit"><div className="font-extrabold text-[var(--navy)]">管理員後台</div><div className="text-xs muted mt-1 break-all">{admin.user.email}</div><nav className="mt-5 grid gap-1"><Link className="px-3 py-2 rounded-lg hover:bg-slate-100 font-semibold" href="/admin">儀表板</Link><Link className="px-3 py-2 rounded-lg hover:bg-slate-100 font-semibold" href="/admin/cases">案例管理</Link><Link className="px-3 py-2 rounded-lg hover:bg-slate-100 font-semibold" href="/admin/cases/new">新增案例</Link></nav><form action={logout} className="mt-5"><button className="btn btn-secondary w-full" type="submit">登出</button></form></aside><section>{children}</section></div></div> }
