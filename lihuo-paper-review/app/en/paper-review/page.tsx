import type { Metadata } from "next";
import Link from "next/link";
export const metadata: Metadata = { title: "Review System" };
const axes=["Structural Reviewability","Premise Integrity","Claim Authority","Formal Completeness","Empirical Support","Ontology Status","Engineering Testability","Failure Boundary Quality","Review Completion"];
export default function Page(){return <div className="container-shell py-14"><div className="kicker">General Review Architecture</div><h1 className="mt-3 text-4xl font-black text-[var(--navy)]">General paper review system</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-700">The system does not prematurely compress a paper into one accept/reject identity. It audits distinct qualification axes and supports PASS, LIMIT, HOLD, PARTIAL, and UNAUDITABLE states.</p><div className="mt-10 grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{axes.map((axis,i)=><div className="card p-5" key={axis}><div className="text-blue-700 font-black">{String(i+1).padStart(2,'0')}</div><div className="mt-2 font-extrabold text-[var(--navy)]">{axis}</div></div>)}</div><div className="mt-10"><Link className="btn btn-primary" href="/en/cases?type=GENERAL_PAPER_REVIEW">Browse general cases</Link></div></div>}
